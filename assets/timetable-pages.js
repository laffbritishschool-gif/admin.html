import { supabase, escapeHtml, toast, setLoading } from './app.js';

const esc=escapeHtml;
const dayName={1:'Monday',2:'Tuesday',3:'Wednesday',4:'Thursday',5:'Friday',6:'Saturday',7:'Sunday'};
const qs=new URLSearchParams(location.search);
const classId=qs.get('class');

function content(html){const shell=document.querySelector('#app-shell');shell.dataset.content=html;const root=document.querySelector('.page-content');if(root)root.innerHTML=html;}
function statusBadge(published){return `<span class="badge ${published?'status-active':'status-inactive'}">${published?'Published':'Not Published'}</span>`;}
function time(v){return v?String(v).slice(0,5):'—';}

async function classes(){
 const {data,error}=await supabase.from('classes').select('id,name,level,description').order('name');
 if(error)throw error;
 const ids=(data||[]).map(x=>x.id);
 let counts={};
 if(ids.length){const {data:e}=await supabase.from('timetables').select('class_id').in('class_id',ids);(e||[]).forEach(x=>counts[x.class_id]=(counts[x.class_id]||0)+1);}
 return (data||[]).map(x=>({...x,count:counts[x.id]||0}));
}

export async function renderTimetablePage(){
 if(classId){return renderClassTimetable(classId);}
 let rows=[];
 content(`<div class="result-hero timetable-hero"><div><span class="hero-kicker">ACADEMIC SCHEDULING</span><h2>Class Timetables</h2><p>Select a class to view and manage its weekly timetable.</p></div><a class="btn" href="timetable.html">Refresh</a></div><div class="panel"><div class="panel-head"><div><h2>All Classes</h2><p>Choose a class to open its timetable.</p></div></div><div id="tt-classes"><div class="skeleton"></div><div class="skeleton"></div></div></div>`);
 try{rows=await classes();document.querySelector('#tt-classes').innerHTML=rows.length?`<div class="tt-class-grid">${rows.map((c,i)=>`<article class="tt-class-card" style="--delay:${i*45}ms"><div class="tt-class-icon">${esc((c.name||'C').slice(0,2).toUpperCase())}</div><div><span class="section-kicker">${esc(c.level||'CLASS')}</span><h3>${esc(c.name)}</h3><p>${c.count} schedule entr${c.count===1?'y':'ies'}</p></div><a class="btn btn-sm" href="timetable.html?class=${encodeURIComponent(c.id)}">View Timetable</a></article>`).join('')}</div>`:'<div class="empty">No classes found.</div>';
 }catch(e){console.error(e);document.querySelector('#tt-classes').innerHTML='<div class="empty">Could not load classes.</div>';toast('Could not load classes.','error');}
}

async function loadSchedules(id){
 const {data,error}=await supabase.from('timetables').select('id,class_id,session_id,term_id,day_of_week,start_time,end_time,subject_id,teacher_id,room,published,created_at,subjects(name,code),teachers(full_name),classes(name,level),academic_sessions(name),terms(name)').eq('class_id',id).order('day_of_week').order('start_time');
 if(error)throw error;return data||[];
}
async function options(table,label){const {data,error}=await supabase.from(table).select(`id,${label}`).order(label);if(error)throw error;return data||[];}
function scheduleForm(row,lists){return `<form id="schedule-form" class="form-grid"><label>Session<select name="session_id" required>${lists.sessions.map(x=>`<option value="${x.id}" ${row?.session_id===x.id?'selected':''}>${esc(x.name)}</option>`).join('')}</select></label><label>Term<select name="term_id">${lists.terms.map(x=>`<option value="${x.id}" ${row?.term_id===x.id?'selected':''}>${esc(x.name)}</option>`).join('')}</select></label><label>Day<select name="day_of_week">${Object.entries(dayName).map(([k,v])=>`<option value="${k}" ${String(row?.day_of_week||1)===k?'selected':''}>${v}</option>`).join('')}</select></label><label>Start<input name="start_time" type="time" value="${time(row?.start_time)==='—'?'':time(row?.start_time)}" required></label><label>End<input name="end_time" type="time" value="${time(row?.end_time)==='—'?'':time(row?.end_time)}" required></label><label>Subject<select name="subject_id" required>${lists.subjects.map(x=>`<option value="${x.id}" ${row?.subject_id===x.id?'selected':''}>${esc(x.name)}${x.code?' — '+esc(x.code):''}</option>`).join('')}</select></label><label>Teacher<select name="teacher_id"><option value="">Unassigned</option>${lists.teachers.map(x=>`<option value="${x.id}" ${row?.teacher_id===x.id?'selected':''}>${esc(x.full_name)}</option>`).join('')}</select></label><label>Room<input name="room" value="${esc(row?.room||'')}"></label><label class="check"><input name="published" type="checkbox" ${row?.published?'checked':''}> Published</label><div class="form-actions"><button class="btn btn-primary" type="submit">${row?'Save Changes':'Add Schedule'}</button><button class="btn btn-ghost" type="button" id="cancel-schedule">Cancel</button></div></form>`}

async function renderClassTimetable(id){
 content(`<div class="detail-topbar"><a class="back-link" href="timetable.html">← All Classes</a></div><div class="result-hero timetable-hero"><div><span class="hero-kicker">WEEKLY SCHEDULE</span><h2 id="class-title">Loading class…</h2><p>Manage, publish and update this class timetable.</p></div><button class="btn" id="add-schedule">+ Add Schedule</button></div><div class="panel"><div class="panel-head"><div><h2>Timetable</h2><p>Edit or remove schedule entries. Publish controls visibility.</p></div><div class="tt-actions"><button class="btn btn-sm" id="publish-all">Publish All</button><button class="btn btn-sm btn-ghost" id="unpublish-all">Unpublish All</button></div></div><div id="schedule-list"><div class="skeleton"></div><div class="skeleton"></div></div></div><div class="panel hidden" id="schedule-editor"><div class="panel-head"><h2 id="editor-title">Add Schedule</h2></div><div id="editor-body"></div></div>`);
 let lists;try{lists={sessions:await options('academic_sessions','name'),terms:await options('terms','name'),subjects:await options('subjects','name'),teachers:await options('teachers','full_name')};const rows=await loadSchedules(id);const title=rows[0]?.classes?.name||'Class Timetable';document.querySelector('#class-title').textContent=title;renderRows(rows,lists,id);wire(id,lists);}catch(e){console.error(e);document.querySelector('#schedule-list').innerHTML='<div class="empty">Could not load this timetable.</div>';toast('Could not load timetable.','error');}}
function renderRows(rows,lists,id){const el=document.querySelector('#schedule-list');if(!rows.length){el.innerHTML='<div class="empty">No timetable entries for this class yet.</div>';return;}el.innerHTML=`<div class="tt-table-wrap"><table class="tt-table"><thead><tr><th>Day</th><th>Time</th><th>Subject</th><th>Teacher</th><th>Room</th><th>Status</th><th>Actions</th></tr></thead><tbody>${rows.map(r=>`<tr><td><strong>${dayName[r.day_of_week]||r.day_of_week}</strong></td><td>${time(r.start_time)} – ${time(r.end_time)}</td><td>${esc(r.subjects?.name||'—')}<small>${esc(r.subjects?.code||'')}</small></td><td>${esc(r.teachers?.full_name||'Unassigned')}</td><td>${esc(r.room||'—')}</td><td>${statusBadge(!!r.published)}</td><td><button class="btn btn-sm btn-ghost edit-tt" data-id="${r.id}">Edit</button> <button class="btn btn-sm btn-ghost toggle-tt" data-id="${r.id}" data-published="${r.published?'1':'0'}">${r.published?'Unpublish':'Publish'}</button> <button class="btn btn-sm btn-danger delete-tt" data-id="${r.id}">Delete</button></td></tr>`).join('')}</tbody></table></div>`;el.querySelectorAll('.edit-tt').forEach(b=>b.onclick=()=>openEditor(rows.find(r=>r.id===b.dataset.id),lists,id));el.querySelectorAll('.toggle-tt').forEach(b=>b.onclick=()=>toggle(b.dataset.id,b.dataset.published!=='1',id));el.querySelectorAll('.delete-tt').forEach(b=>b.onclick=()=>remove(b.dataset.id,id));}
function openEditor(row,lists,id){const ed=document.querySelector('#schedule-editor');ed.classList.remove('hidden');document.querySelector('#editor-title').textContent=row?'Edit Schedule':'Add Schedule';document.querySelector('#editor-body').innerHTML=scheduleForm(row,lists);document.querySelector('#schedule-form').onsubmit=async e=>{e.preventDefault();const btn=e.target.querySelector('button[type=submit]');setLoading(btn,true,'Saving…');const p=Object.fromEntries(new FormData(e.target));p.class_id=id;p.day_of_week=Number(p.day_of_week);p.published=e.target.elements.published.checked;try{const res=row?await supabase.from('timetables').update(p).eq('id',row.id):await supabase.from('timetables').insert(p);if(res.error)throw res.error;toast(row?'Timetable updated.':'Timetable entry added.');ed.classList.add('hidden');await renderClassTimetable(id);}catch(err){toast(err.message||'Could not save timetable.','error')}finally{setLoading(btn,false)}};document.querySelector('#cancel-schedule').onclick=()=>ed.classList.add('hidden');ed.scrollIntoView({behavior:'smooth'});}
async function toggle(id,published,classId){try{const {error}=await supabase.from('timetables').update({published}).eq('id',id);if(error)throw error;toast(published?'Schedule published.':'Schedule unpublished.');await renderClassTimetable(classId);}catch(e){toast(e.message||'Could not update schedule.','error')}}
async function remove(id,classId){if(!confirm('Delete this timetable entry?'))return;try{const {error}=await supabase.from('timetables').delete().eq('id',id);if(error)throw error;toast('Timetable entry deleted.');await renderClassTimetable(classId);}catch(e){toast(e.message||'Could not delete schedule.','error')}}
function wire(id,lists){document.querySelector('#add-schedule').onclick=()=>openEditor(null,lists,id);document.querySelector('#publish-all').onclick=()=>bulk(id,true);document.querySelector('#unpublish-all').onclick=()=>bulk(id,false);}
async function bulk(classId,published){if(!confirm(`${published?'Publish':'Unpublish'} every timetable entry for this class?`))return;try{const {error}=await supabase.from('timetables').update({published}).eq('class_id',classId);if(error)throw error;toast(published?'All timetable entries published.':'All timetable entries unpublished.');await renderClassTimetable(classId);}catch(e){toast(e.message||'Could not update timetable.','error')}}
